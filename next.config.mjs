/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'cdn.serc.carleton.edu',
            'cdn1.byjus.com'
        ],
    },
};

export default nextConfig;
